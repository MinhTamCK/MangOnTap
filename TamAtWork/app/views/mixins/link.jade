mixin link(opts)
  -opts.href = opts.href||'#'
  -opts.text = opts.text||opts.title
  a(href=opts.href, title=opts.title)&attributes(opts.attr||attributes)
    if block
      block
    else
      !=opts.text
